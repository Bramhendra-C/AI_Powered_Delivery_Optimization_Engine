services:
  - type: web
    name: blinkit-order-optimizer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn optimizer_api:app --host 0.0.0.0 --port $PORT
